diff a/python/mas/sse_cost.py b/python/mas/sse_cost.py	(rejected hunks)
@@ -90,20 +90,20 @@ def block_inv(x, is_herm=False):
 
     # precompute inverse of d for efficiency
     d_inv = block_inv(d)
-    t1 = b * d_inv
-    t2 = d_inv * c
-    t3 = b * t2
-    A_inv = block_inv(a - t3)
+    A_inv = block_inv(a - b * d_inv * c)
 
 
     # https://en.wikipedia.org/wiki/Block_matrix#Block_matrix_inversion
     A = A_inv
-    B = -A_inv * t1
+    print(type(A_inv))
+    print(type(b))
+    print(type(d_inv))
+    B = -A_inv * b * d_inv
     if not is_herm:
-        C = -t2 * A_inv
+        C = -d_inv * c * A_inv
     else:
         C = block_herm(B)
-    D = d_inv + t2 * B
+    D = d_inv + d_inv * c * A_inv * b * d_inv
 
     return np.concatenate((np.concatenate((A, B), axis=1),
                            np.concatenate((C, D), axis=1)), axis=0).view(block_array)
